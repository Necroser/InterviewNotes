interface:
  display_name: "Interview Notes"
  short_description: "Turn interview raw folders into Chinese study notes."
  default_prompt: "Use $interview-notes to process this raw audio folder: path/to/raw."
